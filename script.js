window.onload = function () {
  let boxPixelBoard = document.getElementById('pixel-board');
  let onePixelBox = document.querySelector('.pixel');
  
  // for (let index = 0; index < 5; index += 1) {
  //   let box = document.createElement('div');
  //   for (let jindex = 0; jindex < 5; jindex += 1) {
  //     onePixelBox.appendChild(box).className = 'box-pixel';
  //   }
  // }
}
